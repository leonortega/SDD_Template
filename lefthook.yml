pre-commit:
  commands:
    gitleaks-staged:
      run: gitleaks protect --staged --redact

commit-msg:
  commands:
    require-ticket:
      run: python -m tools.sdd_cli dev-flow validate-commit-message "{1}"

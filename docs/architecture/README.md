# Architecture

Documentation about how the system is built: overall architecture, deployment topology, components, data flow, security,
and dependencies.

| File          | Purpose                                 | Typical author | AI updatable |
| ------------- | --------------------------------------- | -------------- | ------------ |
| `system.md`   | Overall architecture, layers, responsibilities, sources of truth | Human + AI | Yes |
| `deployment.md` | Deployment topology, environments, CI/CD, observability | Human + AI | Yes |

Add `components.md`, `data-flow.md`, `security.md`, and `dependencies.md` as the product grows.

# API

API contracts and examples per service. Currently empty because no product stack is selected.

When a product service exists, add `docs/api/<service>.md` with its API contracts and examples. Typical author: AI
(updatable).

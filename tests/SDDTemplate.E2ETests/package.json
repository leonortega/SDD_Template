{
  "name": "sddtemplate-e2e-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install --with-deps chromium",
    "test": "playwright test",
    "test:docker": "cd ../.. && python -m tools.sdd_cli e2e docker --",
    "test:docker:list": "cd ../.. && python -m tools.sdd_cli e2e docker -- --list",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "typescript": "5.9.3"
  }
}

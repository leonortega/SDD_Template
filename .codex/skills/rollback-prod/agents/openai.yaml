interface:
  display_name: "Rollback PROD"
  short_description: "Rollback PROD to a known-good artifact"
  default_prompt: "Use $rollback-prod to redeploy a previously verified PROD artifact and record rollback evidence."

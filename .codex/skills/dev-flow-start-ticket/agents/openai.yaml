interface:
  display_name: "OpenProject Ticket Starter"
  short_description: "Start OpenProject work packages from chat with safe Git branching."
  default_prompt: "Start the next OpenProject Todo ticket."

{
  "schemaVersion": 1,
  "cases": [
    {
      "id": "product-free-shell-status",
      "goal": "Report shell status without routing to product build, deploy, or QA.",
      "expectedRoute": "dev-flow-pipeline-status",
      "assertions": [
        "Recognize that no product stack is selected",
        "Do not invoke product-specific build or QA gates",
        "Report that app targets are unconfigured"
      ]
    }
  ]
}

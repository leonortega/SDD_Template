/**
 * Shared <pkgName> library.
 *
 * Skeleton stub — replace with the real API once a consumer exists.
 */

/** Identify the package from runtime code. */
export function packageName(): string {
  return "<pkgName>";
}

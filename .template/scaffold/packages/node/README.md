# <pkgName>

Shared library for <pkgName> — skeleton stub, not the final API.

Build-time only: consumed by apps via `file:` deps, never deployed. See
`packages/README.md` for the shared-package conventions.

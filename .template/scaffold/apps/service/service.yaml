apiVersion: v1
kind: Service
metadata:
  name: <appId>
spec:
  type: NodePort
  selector:
    app: <appId>
  ports:
    - protocol: TCP
      port: <port>
      targetPort: <port>
      nodePort: <nodePort>   # per-env NodePort from infra/deployment/ports.json (assign-app-ports)

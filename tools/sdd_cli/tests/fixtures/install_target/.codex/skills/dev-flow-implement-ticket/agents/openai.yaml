interface:
  display_name: Implement Ticket
  short_description: Finish OpenProject work package implementation through PR handoff
  default_prompt: Implement the current OpenProject work package from its existing branch and OpenSpec change, satisfy configured quality and coverage gates, create or update the PR, run review, and update OpenProject.

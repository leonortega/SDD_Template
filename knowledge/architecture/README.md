# Architecture

Agent-consulted architecture knowledge (as opposed to `docs/architecture/` which is human documentation). Patterns,
decisions, and constraints discovered during delivery belong here.

Use one file per topic following the standard template in `knowledge/README.md`.

# Fixes

Validated reusable fixes that have been proven in this repository.

Use one file per fix following the standard template in `knowledge/README.md`.

Example: `knowledge/fixes/fix-nuget-cache.md`

name: agentic-nexus

networks:
  nexus:

volumes:
  nexus-data:

services:
  nexus:
    image: sonatype/nexus3:3.92.2
    container_name: agentic-nexus
    ports:
      - "8088:8081"
      - "5000:5000"
    volumes:
      - nexus-data:/nexus-data
    networks:
      - nexus
    restart: unless-stopped

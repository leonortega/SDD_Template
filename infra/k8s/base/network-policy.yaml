apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-openproject
spec:
  podSelector:
    matchLabels:
      app: openproject
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 80

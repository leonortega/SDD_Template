# Placeholder: QA overlay — replace placeholders with QA-specific values
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ${ENV_NAMESPACE}
resources:
  - ../../base
patches:
  - path: config-patch.yaml
images:
  - name: ${REGISTRY}/${COMPONENT_NAME}
    newTag: ${IMAGE_TAG}
# Plane environment template.
#
# Copy this file to infra/plane/variables.env, then replace every
# replace-with-* value before starting Plane.
#
# This file owns only Plane and Plane dependency variables. Monitoring values
# belong in infra/monitoring/variables.env. Azure helper values belong in
# infra/azure/variables.env. Keep real values only in ignored .env files.

# ---------------------------------------------------------------------------
# Shared paths and public local URLs
# ---------------------------------------------------------------------------
INSTALL_DIR=/opt/plane
DOMAIN_NAME=agentic.lvh.me
SITE_ADDRESS=agentic.lvh.me:8080
WEB_URL=http://agentic.lvh.me:8080
CORS_ALLOWED_ORIGINS=http://agentic.lvh.me:8080,http://localhost:8080
APP_RELEASE_VERSION=v2.6.0
IS_AIRGAPPED=0
IS_SELF_MANAGED=1

# ---------------------------------------------------------------------------
# Plane ingress and runtime
# ---------------------------------------------------------------------------
LISTEN_HTTP_PORT=8080
LISTEN_HTTPS_PORT=443
LISTEN_SMTP_PORT_25=10025
LISTEN_SMTP_PORT_465=10465
LISTEN_SMTP_PORT_587=10587
APP_PROTOCOL=http
TRUSTED_PROXIES=0.0.0.0/0
API_HOSTNAME=http://api:8000
API_BASE_URL=http://api:8000
LIVE_BASE_URL=http://live:3000
LIVE_BASE_PATH=/live
PI_BASE_PATH=/pi
PLANE_API_HOST=http://agentic.lvh.me:8080
PLANE_OAUTH_REDIRECT_URI=http://agentic.lvh.me:8080/pi/api/v1/oauth/callback/
PLANE_OAUTH_STATE_EXPIRY_SECONDS=82800
DEBUG=0
SSL_VERIFY=1
ENABLE_DRF_SPECTACULAR=0
API_KEY_RATE_LIMIT=60/minute
FEATURE_FLAG_SERVER_BASE_URL=http://monitor:8080
PAYMENT_SERVER_BASE_URL=http://monitor:8080
IFRAMELY_URL=http://iframely:8061

# ---------------------------------------------------------------------------
# Plane service replica counts
# ---------------------------------------------------------------------------
WEB_REPLICAS=1
SPACE_REPLICAS=1
ADMIN_REPLICAS=1
API_REPLICAS=1
WORKER_REPLICAS=1
BEAT_WORKER_REPLICAS=1
LIVE_REPLICAS=1
SILO_REPLICAS=1
EMAIL_REPLICAS=0
OUTBOX_POLLER_REPLICAS=1
AUTOMATION_CONSUMER_REPLICAS=1
IFRAMELY_REPLICAS=1
PI_API_REPLICAS=0
PI_BEAT_REPLICAS=0
PI_WORKER_REPLICAS=0
PI_MIGRATOR_REPLICAS=0

# ---------------------------------------------------------------------------
# Required generated local secrets
# ---------------------------------------------------------------------------
# Generate strong local values before use:
#   [Convert]::ToHexString([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32)).ToLower()
MACHINE_SIGNATURE=replace-with-generated-machine-signature
SECRET_KEY=replace-with-plane-secret-key
SILO_HMAC_SECRET_KEY=replace-with-silo-hmac-secret
AES_SECRET_KEY=replace-with-aes-secret
LIVE_SERVER_SECRET_KEY=replace-with-live-server-secret
PI_INTERNAL_SECRET=replace-with-pi-internal-secret
WEBHOOK_SECRET=replace-with-webhook-secret

# ---------------------------------------------------------------------------
# PostgreSQL
# ---------------------------------------------------------------------------
POSTGRES_USER=plane
POSTGRES_PASSWORD=replace-with-postgres-password
POSTGRES_DB=plane
PGHOST=plane-db
PGDATABASE=plane
PGDATA=/var/lib/postgresql/data
DATABASE_URL=postgresql://plane:replace-with-postgres-password@plane-db/plane
PLANE_PI_DATABASE_URL=postgresql://plane:replace-with-postgres-password@plane-db/plane_pi
FOLLOWER_POSTGRES_URI=postgresql://plane:replace-with-postgres-password@plane-db/plane

# ---------------------------------------------------------------------------
# Redis
# ---------------------------------------------------------------------------
REDIS_HOST=plane-redis
REDIS_PORT=6379
REDIS_URL=redis://plane-redis:6379/

# ---------------------------------------------------------------------------
# RabbitMQ
# ---------------------------------------------------------------------------
RABBITMQ_HOST=plane-mq
RABBITMQ_PORT=5672
RABBITMQ_DEFAULT_USER=plane
RABBITMQ_DEFAULT_PASS=replace-with-rabbitmq-password
RABBITMQ_PASSWORD=replace-with-rabbitmq-password
RABBITMQ_DEFAULT_VHOST=plane
RABBITMQ_VHOST=plane
AMQP_URL=amqp://plane:replace-with-rabbitmq-password@plane-mq:5672/plane
MQ_PREFETCH_COUNT=10

# ---------------------------------------------------------------------------
# MinIO and object storage
# ---------------------------------------------------------------------------
USE_MINIO=1
USE_STORAGE_PROXY=0
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=replace-with-minio-access-key
AWS_SECRET_ACCESS_KEY=replace-with-minio-secret-key
AWS_S3_ENDPOINT_URL=http://plane-minio:9000
AWS_S3_BUCKET_NAME=uploads
MINIO_ROOT_USER=replace-with-minio-access-key
MINIO_ROOT_PASSWORD=replace-with-minio-secret-key
MINIO_ENDPOINT_SSL=0
BUCKET_NAME=uploads
FILE_SIZE_LIMIT=5242880
MAX_ATTACHMENT_SIZE=10485760

# ---------------------------------------------------------------------------
# OpenSearch and embeddings
# ---------------------------------------------------------------------------
OPENSEARCH_ENABLED=0
OPENSEARCH_URL=
OPENSEARCH_USERNAME=
OPENSEARCH_PASSWORD=
OPENSEARCH_INDEX_PREFIX=
EMBEDDING_MODEL=
EMBEDDING_MODEL_ID=

# ---------------------------------------------------------------------------
# Certificates and email intake
# ---------------------------------------------------------------------------
CERT_EMAIL=admin@example.com
CERT_ACME_CA=https://acme-v02.api.letsencrypt.org/directory
CERT_ACME_DNS=
SMTP_DOMAIN=0.0.0.0
TLS_CERT_PATH=
TLS_PRIV_KEY_PATH=
INTAKE_EMAIL_DOMAIN=example.com

# ---------------------------------------------------------------------------
# Optional integrations
# ---------------------------------------------------------------------------
INTEGRATION_CALLBACK_BASE_URL=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_APP_NAME=
GITHUB_APP_ID=
GITHUB_PRIVATE_KEY=
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=

# ---------------------------------------------------------------------------
# Plane AI and model integrations
# ---------------------------------------------------------------------------
OPENAI_API_KEY=
OPENAI_BASE_URL=
CLAUDE_API_KEY=
CLAUDE_BASE_URL=
GROQ_API_KEY=
GROQ_BASE_URL=
COHERE_API_KEY=
COHERE_BASE_URL=
CUSTOM_LLM_ENABLED=false
CUSTOM_LLM_MODEL_KEY=replace-with-custom-llm-model-key
CUSTOM_LLM_BASE_URL=
CUSTOM_LLM_API_KEY=
CUSTOM_LLM_NAME=replace-with-custom-llm-name
CUSTOM_LLM_DESCRIPTION=replace-with-custom-llm-description
CUSTOM_LLM_MAX_TOKENS=128000
BR_AWS_ACCESS_KEY_ID=
BR_AWS_SECRET_ACCESS_KEY=
BR_AWS_SESSION_TOKEN=

# ---------------------------------------------------------------------------
# Plane workers and queues
# ---------------------------------------------------------------------------
GUNICORN_WORKERS=2
BATCH_SIZE=60
DEDUP_INTERVAL=3
SILO_BASE_PATH=/silo
FASTAPI_APP_HOST=0.0.0.0
FASTAPI_APP_WORKERS=1
OUTBOX_POLLER_MEMORY_LIMIT_MB=512
OUTBOX_POLLER_INTERVAL_MIN=0.25
OUTBOX_POLLER_INTERVAL_MAX=2
OUTBOX_POLLER_BATCH_SIZE=250
OUTBOX_POLLER_MEMORY_CHECK_INTERVAL=30
OUTBOX_POLLER_POOL_SIZE=4
OUTBOX_POLLER_POOL_MIN_SIZE=2
OUTBOX_POLLER_POOL_MAX_SIZE=10
OUTBOX_POLLER_POOL_TIMEOUT=30.0
OUTBOX_POLLER_POOL_MAX_IDLE=300.0
OUTBOX_POLLER_POOL_MAX_LIFETIME=3600
OUTBOX_POLLER_POOL_RECONNECT_TIMEOUT=5.0
OUTBOX_POLLER_POOL_HEALTH_CHECK_INTERVAL=60
AUTOMATION_EVENT_STREAM_QUEUE_NAME=plane.event_stream.automations
AUTOMATION_EVENT_STREAM_PREFETCH=10
AUTOMATION_EXCHANGE_NAME=plane.event_stream
CELERY_VECTOR_SYNC_ENABLED=0
CELERY_VECTOR_SYNC_INTERVAL=3
CELERY_WORKSPACE_PLAN_SYNC_ENABLED=0
CELERY_WORKSPACE_PLAN_SYNC_INTERVAL=86400
CELERY_DOCS_SYNC_ENABLED=0
CELERY_DOCS_SYNC_INTERVAL=86400
